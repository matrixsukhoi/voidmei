//! 网络端口小工具 (A3 收敛): 备份端口算术的单一真相。

/// 备用端口 = 主端口 + 1111 (主/备端口差, 缺省 8111→9222)。
/// u16 饱和加法: 主端口 > 64424 时钳在 65535, 不回绕不 panic
/// (Java int 静默回绕形态不移植; 域内 httpPort 缺省 8111, 溢出仅理论可达)。
pub fn bkp_port(port: u16) -> u16 {
    port.saturating_add(1111)
}
