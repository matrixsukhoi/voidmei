[package]
name = "vm-app"
version.workspace = true
edition.workspace = true
description = "VoidMei 应用组装 bin (Controller/生命周期/MainForm 入口)"

[lib]
name = "vm_app"

[[bin]]
name = "voidmei"
path = "src/main.rs"

[dependencies]
vm-core = { workspace = true }
vm-data = { workspace = true }
vm-overlay = { workspace = true }
vm-ui = { workspace = true }
vm-webui = { workspace = true }
# D9 表单 IPC dispatcher 的 dto 序列化 + 主循环事件 emit (Emitter trait)
serde_json = { workspace = true }
tauri = "2"

# 语音子系统装配批: winmm waveOut 播放器 (winmm_player.rs)
# feature 写法对齐 vm-overlay Cargo.toml (基线见 workspace 根)
[target.'cfg(windows)'.dependencies]
windows = { workspace = true, features = ["Win32_Media_Audio"] }

[lints]
workspace = true
